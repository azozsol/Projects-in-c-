#include <catch2/catch.hpp>
#include <Game.h>
using namespace AllPi;



TEST_CASE("test class Game, isDemineurAttBomb") {
/**
    Game game{};
    Position p1{0,0}; Bomb bomb{p1};
    Position p2{0,1}; Demineur demineur{p2};
    Position p3{0,2}; Flag flag{p3};
    Position p4{0,3}; Sergent sergent{p4};

    SECTION("demineur attack bomb"){
        bool expected = true;
        bool result = game.dimeneurAttBomb(demineur, bomb);
        REQUIRE(expected == result);
    }

    SECTION("sergent attack bomb"){
        bool expected = false;
        bool result = game.dimeneurAttBomb(sergent, bomb);
        REQUIRE(expected == result);
    }

    SECTION("bomb attack bomb"){
        bool expected = false;
        bool result = game.dimeneurAttBomb(bomb, bomb);
        REQUIRE(expected == result);
    }

    SECTION("bomb attack demineur"){
        bool expected = false;
        bool result = game.dimeneurAttBomb(bomb, demineur);
        REQUIRE(expected == result);
    }
    */
}
